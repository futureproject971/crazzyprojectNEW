import { LoadingState } from "@/core/design-system";

export default function CommunityLoading() {
  return (
    <main className="crz-community-page crz-community-state">
      <LoadingState label="Entrando na comunidade..." />
    </main>
  );
}
