import { LoadingState } from "@/core/design-system";

export default function ComboLoading() {
  return (
    <main className="crz-combo-page crz-cart-route-state">
      <LoadingState label="Preparando o Combo CRAZZY..." />
    </main>
  );
}
